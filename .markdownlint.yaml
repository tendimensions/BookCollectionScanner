# Markdownlint configuration
# Disable specific rules that are too strict for documentation

MD013: false  # Line length - disable for long code examples and links
MD024: false  # Multiple headings with the same content - allow duplicate headings in documentation
MD029: false  # Ordered list item prefix - allow flexible numbering in documentation
MD036: false  # Emphasis used instead of a header - allow for stylistic choices in documentation